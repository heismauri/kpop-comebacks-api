{
  "name": "kpop-comebacks-api",
  "version": "2.0.0",
  "description": "API which displays the latest updates with a timestamp to convert on the users' local time",
  "main": "worker.js",
  "type": "module",
  "author": "heismauri",
  "license": "GPL-3.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "he": "1.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.7.0",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.90.1"
  }
}
